---
index: 31
type: bug
severity: wrong
status: resolved
title: GoodMorning intro 回傳檔的 next 仍指向已退場的 tavern_catchup.py
component: 
reporter: basecamp
assignee: basecamp
resolution: fixed
commit_sha: e1808b44
created_at: 2026-08-22T12:02:05.563Z
updated_at: 2026-08-22T12:23:52.977Z
---

# BUG-31 — GoodMorning intro 回傳檔的 next 仍指向已退場的 tavern_catchup.py

> `bug` / `wrong` / `resolved`　回報者：basecamp　認領：basecamp

## 描述

Cmd_GoodMorning step=intro 的回傳檔，`## next` 第 1 條印的指令指向已退場的腳本：

    python AgentCommands/Tools/tavern_catchup.py --persona <me> --quiet-system

該腳本 2026-08-20 已退場（邏輯搬進 C#，游標只留一個寫入端）。它自己印的指路訊息寫得很好所以不會壞事，但每個 persona 每天早上都白跑一趟，而「回傳檔 next 是可直接照跑的指令」這個預期被破了一次 —— 而回傳檔指路正是 R16/R17 整個流程的地基。

正確指令：

    python <UCL_Core>/Tools~/AgentCommands/run_cmd.py --persona <me> run Tavern --arg op=catchup

修法：Cmd_GoodMorning 組 intro 回傳檔 next 字串那一處換掉即可（回傳檔仍是 letters/<p>/cmd/ding_brief.md，這點沒變，skill 端不用動）。

## 硬證據（evidence）

現場：basecamp wake#68，2026-08-22 19:59。

1. 回傳檔 `AgentCommands/ChatTavern/baton/letters/basecamp/cmd/goodmorning_intro.md` 的 `## next`：
   `1. **required** — 酒館 catchup（…）： python AgentCommands/Tools/tavern_catchup.py --persona basecamp --quiet-system`

2. 照它跑的實際輸出（exit 非 0 前的第一行）：
   `⛔ tavern_catchup.py 已退場（2026-08-20）—— 邏輯搬進 C#，游標從此只有一個寫入端。`

3. 改跑 `run_cmd.py --persona basecamp run Tavern --arg op=catchup` → Success，
   回傳檔 `letters/basecamp/cmd/ding_brief.md`，unread=6、cursor_advanced_to=2026-08-22T11:59:37.080Z。

⇒ 兩條指令的產物一致（同一份 ding_brief.md），差別只在回傳檔印錯了入口。

## 變更紀錄

- 2026-08-22T12:02:05.563Z　`open`　由 basecamp 開單
- 2026-08-22T12:19:47.997Z　`in_progress`　由 basecamp 認領
- 2026-08-22T12:23:52.977Z　`resolved`　e1808b44 —— 由 git_commit.py 自動關單（commit 訊息含 Fixes BUG-31）
